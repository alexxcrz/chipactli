{
  "type": "module",
  "name": "chipactli",
  "version": "1.0.0",
  "description": "Sistema de control de insumos para cosmética sólida - Chipactli",
  "scripts": {
    "dev": "cd backend && node server.js",
    "dev:backend": "cd backend && node server.js",
    "start": "cd backend && node server.js",
    "install:all": "npm install && cd backend && npm install",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["chipactli", "inventario", "control-insumos"],
  "author": "alexxcrz",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "serve": "^14.2.5"
  },
  "dependencies": {
    "open": "^11.0.0"
  }
}
